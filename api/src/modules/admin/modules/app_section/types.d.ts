export type app_sectionFilterType = {
  published: boolean;
};

export type app_sectionUpdate = {
  title: string;
  title_en: string;
  published: boolean;
};

export type eventFilterType = {
  published: boolean;
};
